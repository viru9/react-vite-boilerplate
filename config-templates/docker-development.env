# Frontend Docker → Backend Docker Development
# Use this when running both frontend and backend in Docker
# Frontend: docker-compose up -d (port 5173)
# Backend: docker-compose up -d (port 3000)
# Copy this to .env when needed

VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_WS_BASE_URL=ws://localhost:3000
VITE_API_TIMEOUT=10000
VITE_TOKEN_KEY=auth_token
VITE_REFRESH_TOKEN_KEY=refresh_token
VITE_APP_NAME=React Boilerplate - Docker
