# Production Native Deployment
# Use this for production deployment without Docker
# Frontend: Served by Nginx/Apache on port 80/443
# Backend: Running on port 3000 behind reverse proxy
# Copy this to .env for production builds

VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
VITE_WS_BASE_URL=wss://api.yourdomain.com
VITE_API_TIMEOUT=30000
VITE_TOKEN_KEY=auth_token
VITE_REFRESH_TOKEN_KEY=refresh_token
VITE_APP_NAME=React Boilerplate
