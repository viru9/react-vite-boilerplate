# Production Docker Deployment
# Use this for production deployment with Docker
# Frontend: Docker container on port 80, served via reverse proxy
# Backend: Docker container on port 8000, served via reverse proxy
# Copy this to .env for Docker production builds

VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
VITE_WS_BASE_URL=wss://api.yourdomain.com
VITE_API_TIMEOUT=30000
VITE_TOKEN_KEY=auth_token
VITE_REFRESH_TOKEN_KEY=refresh_token
VITE_APP_NAME=React Boilerplate
